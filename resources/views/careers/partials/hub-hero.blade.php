<x-public.hero class="medca-hero-gradient text-white">
    <p class="text-xs font-semibold uppercase tracking-[0.2em] text-white/80">{{ config('careers.organization_name') }}</p>
    <h1 class="mt-4 text-3xl font-semibold leading-tight md:text-5xl">{{ __('Careers at MEDCA Consultancy') }}</h1>
    <p class="mt-5 max-w-2xl text-base leading-relaxed text-white/85 md:text-lg">
        {{ __('Join our clinical and operations teams across India. Structured roles, clear locations, and transparent application paths.') }}
    </p>
</x-public.hero>

<section class="mom-card p-6">
    <h2 class="mom-section-title">{{ __('Search Console') }}</h2>
    <p class="mom-body-text mt-2 text-[var(--text-secondary)]">{{ __('Winning and opportunity queries from Google Search Console API.') }}</p>
    <div class="mt-6">
        @livewire('growth.gsc-dashboard')
    </div>
</section>
